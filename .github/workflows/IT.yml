name: IT

on: [pull_request]

jobs:
  test_it:

    runs-on: ubuntu-latest
    
    steps:
    - name: Install Scala
      working-directory: server
      run: sudo echo $PATH
    - name: Checking directory
      run: sudo echo $PATH
