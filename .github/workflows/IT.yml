name: IT

on: [pull_request]

jobs:
  test_it:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Install Scala
      run: |
        pwd
        cd server
        pwd
    - name: Checking directory
      run: pwd
