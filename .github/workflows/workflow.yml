name: CI

on: [pull_request]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: echo this is one line
    - name: Run sbt compile
      run: |
        echo pwd
        cd server
        sbt
        util/compile
        util/test
        kafkaAssigner/compile
        kafkaAssigner/test
